<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Target 36 is deliberate until Fold8 field validation. -->
    <issue id="OldTargetApi" severity="ignore" />
    <!-- Newer AndroidX lines require API 37, which is not in the public stable sdkmanager channel. -->
    <issue id="GradleDependency" severity="ignore" />
    <!-- Gradle 9.3.1 is the AGP 9.1.1-compatible, locally verified wrapper for this release. -->
    <issue id="AndroidGradlePluginVersion" severity="ignore" />
</lint>
