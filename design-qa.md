# v1.4.0 Design QA

## 비교 대상

- 공개 최종 실기기 화면: [`docs/images/app-home-ko-v1.4.0.png`](docs/images/app-home-ko-v1.4.0.png)
- 공개 프리셋 선택 화면: [`docs/images/app-select-offset-ko-v1.4.0.png`](docs/images/app-select-offset-ko-v1.4.0.png)
- 원본 기준 시안·픽셀 비교 이미지는 개인정보가 포함될 수 있는 로컬 `build-evidence/`에 보관하며 Git에서 제외
- 기기: Galaxy Z Fold8 커버 화면, Android logical display `0`
- 화면 조건: `1248 x 1972`, `420 dpi`, 글꼴 배율 `1.0`, 세로 방향
- 비교 상태: 저장·실제 적용값 `+50`, 화면에서 적용 전 초안 `0` 선택

시안과 실제 커버 화면의 종횡비가 다르므로 전체 높이를 억지로 맞추지 않고, 숫자·광원·슬라이더·프리셋·주요 버튼 영역을 각각 원래 비율로 잘라 같은 이미지 안에서 비교했습니다.

## 요구사항 확인

| 항목 | 확인 결과 | 판정 |
|---|---|---|
| 메인 숫자 후광 | 파랑 20dp 외곽광, 시안 8dp 근접광, 흰색 코어가 실제 화면에서 구분됨 | PASS |
| 손잡이 후광 | 28dp 코어 밖으로 파랑·시안 후광이 퍼지고 부모에 잘리지 않음 | PASS |
| 막대 연속성 | 손잡이 아래에서 활성·비활성 트랙이 끊기지 않음 | PASS |
| 막대 좌우 여백 | 64dp 손잡이 레이아웃을 폐기하고 28dp 측정 크기+무제한 블러로 바꿔 양끝 과도한 공백 제거 | PASS |
| 프리셋 세분화 | `-75/-50/-25/0/+25/+50/+75` 7개가 한 줄에 잘림 없이 표시됨 | PASS |
| 프리셋 중첩 사각형 | Material 기본 버튼을 자체 Surface로 교체하여 숫자 뒤 중첩 판이 사라짐 | PASS |
| 주요 버튼 | 가로·세로 그라데이션, 외곽선, 상단 하이라이트, 외곽 후광이 표시됨 | PASS |
| 하단 기능 | 재부팅 후 재적용, 원래 값 복원, 기기 진단의 문구·아이콘·구분선이 잘림 없이 표시됨 | PASS |
| 상태 구분 | 선택 프리셋, 실제 적용값, 적용 가능한 버튼 상태가 색과 문구로 함께 구분됨 | PASS |

## 반복 수정 기록

1. v1.3.1 캡처에서 숫자·손잡이 후광 부재, 손잡이 좌우 막대 공백, 선택 프리셋 숫자 뒤 중첩 사각형을 확인했습니다.
2. 첫 v1.4.0 구현에서 자체 트랙과 64dp 후광 손잡이를 적용했지만, 손잡이 측정 폭 때문에 막대 양끝 여백이 커지는 문제를 코드 검토로 발견했습니다.
3. 손잡이 측정 크기를 28dp로 되돌리고 무제한 블러만 바깥으로 그리도록 변경했습니다.
4. 기준 시안과 실기기 화면을 같은 비교 이미지에 넣어 숫자와 손잡이 후광이 약한 P2 차이를 확인하고 불투명도를 한 차례 높였습니다.
5. 최종 실기기 캡처에서 P0/P1/P2 미해결 항목이 없음을 확인했습니다. 7개 프리셋이 시안의 4개보다 조밀한 것은 사용자 요청 기능을 반영한 의도된 차이입니다.

## 최종 판정

final result: passed
